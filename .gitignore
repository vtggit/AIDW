# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
dist/
build/
.eggs/

# Python tool caches (local only, never commit)
.ruff_cache/
.pytest_cache/
.mypy_cache/
.tox/

# Virtual environments
.venv/
venv/
env/

# Environment files (never commit secrets)
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Docker named volumes (local data)
# (not tracked — managed by docker)

# Logs
*.log

# Node (if frontend ever gets a package manager)
node_modules/
package-lock.json
yarn.lock

# Playwright / E2E artifacts (per-run harness output)
test-results/

# Playwright junit artifact (generated per run)
app/tests/report.xml

# Internal working docs — strategy / topology / customer detail (NEVER commit to the public repo)
# Add any new internal scoping/handover doc here.
/HANDOVER.md
/RTBF-UI-SCOPE.md
/ASSISTANT-ARCHITECTURE.md
/ASSISTANT-PHASE1.md
/ASSISTANT-PHASE2.md
/ASSISTANT-PHASE3.md
/ASSISTANT-PHASE4.md
/DEMO-RUNBOOK.md
