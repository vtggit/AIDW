"""Dashboards + the accept/dismiss actions that turn suggestions into dashboard items.

``service`` holds the accept (copy a suggestion 1:1 into a dashboard_item + its role-tagged fields,
mark the suggestion accepted) and dismiss actions. The dashboards / dashboard_items /
dashboard_item_fields tables are plain CRUD entities alongside.
"""
