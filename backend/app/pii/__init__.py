"""The PII watchdog (governance pillar #75).

Two-tier deterministic classification over discovered schema — a structural clone of the
suggestion engine (doc §10): ``engine`` is the pure schema-tier rule pass (this package's first
module); the profile-tier value detectors and the fingerprint reconciler follow as their own
small modules per the pillar's lane decomposition.
"""
